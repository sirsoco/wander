import React from 'react';
import firebaseAuthentication from '../firebase.js'

export default function authentication(props) {
    return (
        <div>
            <button className="button" >{onClick(auth)}</button>
        </div>
    )
}
